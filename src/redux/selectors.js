export const getContacts = state => state.contacts;

export const initialFilter = state => state.filter;

export const getName = state => state.form.name;

export const getNumber = state => state.form.number;
